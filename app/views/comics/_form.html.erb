<%= form_with(model: comic, local: true) do |form| %>
  <% if comic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comic.errors.count, "error") %> exist</h2>

      <ul>
        <% comic.errors.full_messages.each do |message| %>
          <li class="alert-danger"><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form_group">
    <%= form.label :year %>
    <%= form.number_field :year, class: "form-control" %>
  </div>

  <div class="field form_group">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div class="field form_group">
    <%= form.label :abstract %>
    <%= form.text_area :abstract, class: "form-control" %>
  </div>

  <div class="field form_group">
    <%= form.label :status %>
    <%= form.select :status ,["todo", "doing", "done"] ,{} , class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "Add Comic Lists", class: "btn btn-primary mt-2" %>
  </div>
<% end %>
